# Ignore uploaded media (keep the folder + protective files)
uploads/*
!uploads/.htaccess
!uploads/index.html

# OS / editor noise
.DS_Store
Thumbs.db
*.log

# Local config overrides written by install.php
config.local.php
install.lock
